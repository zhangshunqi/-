#include <stdio.h>
#include<stdlib.h>
#include <time.h>
int main()
{
    srand((int)time(NULL));
    int n = rand() % 6 + 1;
    printf("Dice is: %d\n", n);
    if (n % 2 == 0) {
        printf("win\n");
    } else {
        printf("lose\n");
    }
	return 0;
}
